思路想法：


看到OpenCV图像加法想出一种异常检测方法：

	将流水线图像加和，取平均，如果差异大，则认为错误。

	逐张操作，

	对每张，跟踪监测，roi然后，像素对齐，（选择区域像素最接近的区域对齐）然后做差。

		采用图像混合的方法，前面的图像平均值权重值设小点，比如0.4,或者0.45，后面新入待测图像权重为0.6或者0.55。以此来测试。

			dst = alpha * img1 + beta * img2 + bias


	for i in range(N)：
		add i
		average
		if i -average >=threshold:
			return wrong.
		else:
			continue



	细节：
		对于图像左右不一致，翻转一下。



第二种想法：

	通过边缘轮廓检测，然后，对杂质人工剔除，然后作为生成图提取特征，这样数据量减小很多。

	通过原图和轮廓图，用GAN生成。

	然后对生成图进行精确分类。

	输入是原图：输出是轮廓图。第一个模型。

	然后对所有图像的轮廓训练第二个模型，作为分类模型。



